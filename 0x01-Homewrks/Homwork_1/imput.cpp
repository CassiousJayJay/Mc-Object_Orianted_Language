#include <iostream>

int main() {
    // declaring the variable
    int num; 
    // printing out the message to the console
    std::cout << "Enter an integer: ";
    std::cin >> num;   // Taking input
    // assigning the input from the user and assigning them to the variable num.
    std::cout << "The number is: " << num;
    return 0;
}